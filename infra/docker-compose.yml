services:
  app:
    build:
      context: ..
      dockerfile: infra/Dockerfile
    env_file:
      - ../.env.example
    volumes:
      - ../:/app
    command: python -m pytest -q

  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"

