# Skills

This folder hosts **runtime Agent Skills** (interfaces + implementations) used by the Chimera system.

## Conventions
- Keep each skill in its own subfolder.
- Prefer pure functions and small, testable modules.
- Document inputs/outputs and failure modes in each skill package.

## Skill stubs
- `trend_fetch/`
- `generate_content/`
- `post_content/`

